{
  "name": "ui-events-app",
  "version": "1.0.0",
  "description": "UI Events Application",
  "private": true,
  "workspaces": [
    "frontend",
    "functions"
  ],
  "scripts": {
    "dev": "concurrently \"pnpm --filter frontend dev\" \"pnpm --filter functions dev\"",
    "build": "pnpm --filter frontend build && pnpm --filter functions build",
    "test": "pnpm --filter frontend test && pnpm --filter functions test",
    "lint": "pnpm --filter frontend lint && pnpm --filter functions lint"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
